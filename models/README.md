# Models

This directory contains trained model weights.

- weights/ - Trained model weights
- checkpoints/ - Training checkpoints

Default runtime checkpoint:

- checkpoints/best.ckpt - CardioFormer checkpoint loaded automatically by the backend
