# 🚀 部署助手 - 逐步操作指南

**你的情况**: 已有服务器
**目标**: 部署ECG智能诊断系统
**方式**: 逐步指导操作

---

## 📋 部署前确认

在开始之前，请确认以下信息：

### ❓ 请告诉我你的服务器信息

**需要的信息**:

1. **服务器操作系统**？
   - Ubuntu 20.04 / 22.04
   - CentOS 7 / 8
   - Debian 10 / 11
   - 其他

2. **服务器配置**？
   - CPU核心数
   - 内存大小
   - 硬盘空间

3. **你能SSH连接到服务器吗**？
   - 能连接
   - 不确定如何连接
   - 需要帮助

4. **你的服务器公网IP**？
   - 例如: 123.45.67.89

---

## 🚀 部署方式选择

### 方式A: 使用一键部署脚本 ⭐⭐⭐⭐⭐（推荐）

**前置条件**: 服务器已安装 Docker 和 Docker Compose。
如果没有，先执行：
```bash
curl -fsSL https://get.docker.com | bash
```

**你只需要**:
1. SSH连接到服务器
2. 运行一个命令
3. 等待完成

**操作步骤**:
```bash
# 1. 连接到服务器
ssh root@你的服务器IP

# 2. 克隆项目
git clone https://github.com/suizhaoqinggong/ECG-Diagnosis-Suite.git
cd ECG-Diagnosis-Suite

# 3. 一键部署（首次运行自动生成配置）
bash deploy.sh

# 等待3-5分钟自动完成部署
```

---

### 方式B: 手动逐步操作 ⭐⭐⭐

**我来指导你每一步**

#### 第一步：连接服务器

```bash
# 在你的本地终端执行
ssh root@你的服务器IP

# 输入密码（不会显示，直接输入后按回车）
```

**遇到问题？**
- 提示"Connection refused": 检查安全组22端口是否开放
- 提示"Permission denied": 密码错误或使用密钥登录

---

#### 第二步：更新系统

连接成功后，执行：

```bash
# 更新软件包列表
apt update

# 升级已安装的包
apt upgrade -y

# 安装必要工具
apt install -y git curl wget vim
```

**如果系统不是Ubuntu**:
- CentOS: `yum update -y`
- Debian: 同Ubuntu

---

#### 第三步：安装Docker

```bash
# 安装Docker
curl -fsSL https://get.docker.com | bash

# 启动Docker
systemctl start docker
systemctl enable docker

# 验证安装
docker --version
```

**预期输出**: `Docker version 24.0.x, build xxxxxxx`

---

#### 第四步：安装Docker Compose

```bash
# 安装Docker Compose
apt install -y docker-compose

# 验证安装
docker-compose --version
```

---

#### 第五步：获取项目代码

```bash
# 创建目录
mkdir -p /opt/projects
cd /opt/projects

# 克隆项目
git clone https://github.com/suizhaoqinggong/ECG-Diagnosis-Suite.git

# 进入项目目录
cd ECG-Diagnosis-Suite
```

---

#### 第六步：部署项目

```bash
# 确保在项目目录
cd /opt/projects/ECG-Diagnosis-Suite

# 一键部署（首次运行会自动生成配置文件）
bash deploy.sh

# 查看启动状态
docker compose -f docker-compose.prod.yml ps

# 查看日志（如果有问题）
docker compose -f docker-compose.prod.yml logs -f
```

**预期看到**:
```
NAME                STATUS    PORTS
ecg-backend         running   8000/tcp
ecg-frontend        running   80/tcp
ecg-db              running   3306/tcp
ecg-reverse-proxy   running   0.0.0.0:80->80/tcp
```

---

#### 第八步：验证部署

```bash
# 通过反向代理测试后端（生产环境后端不直接暴露端口）
curl http://localhost/api/health

# 预期输出: {"status":"healthy"}
```

---

## 🌐 访问你的应用

部署成功后，在浏览器中访问：

```
前端界面: http://你的服务器IP
后端API: http://你的服务器IP/api/docs
```

---

## 🔧 常用运维命令

### 查看服务状态
```bash
docker compose -f docker-compose.prod.yml ps
```

### 查看日志
```bash
# 查看所有服务日志
docker compose -f docker-compose.prod.yml logs -f

# 查看特定服务日志
docker compose -f docker-compose.prod.yml logs -f backend
docker compose -f docker-compose.prod.yml logs -f frontend
```

### 重启服务
```bash
docker compose -f docker-compose.prod.yml restart
```

### 停止服务
```bash
docker compose -f docker-compose.prod.yml down
```

### 更新代码
```bash
git pull
docker compose -f docker-compose.prod.yml up -d --build
```

---

## 🐛 常见问题排查

### 问题1: 端口无法访问

**检查**:
```bash
# 检查端口监听
netstat -tuln | grep -E '(80|443)'

# 检查防火墙
ufw status

# 开放端口（如果需要）
ufw allow 80
ufw allow 443
```

**云服务器检查**:
- 登录云服务商控制台
- 找到"安全组"配置
- 开放80和443端口

---

### 问题2: Docker服务启动失败

```bash
# 查看Docker状态
systemctl status docker

# 重启Docker
systemctl restart docker

# 查看日志
journalctl -u docker
```

---

### 问题3: 容器启动失败

```bash
# 查看容器日志
docker compose -f docker-compose.prod.yml logs

# 检查配置
docker compose -f docker-compose.prod.yml config

# 重新构建
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml build --no-cache
docker compose -f docker-compose.prod.yml up -d
```

---

### 问题4: 内存不足

```bash
# 查看内存
free -h

# 如果内存不足，创建swap
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
```

---

## 📞 接下来怎么做？

**现在告诉我**:

1. **你的服务器操作系统**（Ubuntu/CentOS/其他）
2. **你想使用哪种部署方式**:
   - A. 一键脚本（推荐，快速）
   - B. 手动操作（学习理解）

3. **或者直接告诉我**: "我已连接到服务器，开始部署"

**我会根据你的情况提供具体的命令指导！** 🚀

---

**创建时间**: 2026-03-12
**脚本位置**: `deploy.sh`
**状态**: ✅ 准备就绪
