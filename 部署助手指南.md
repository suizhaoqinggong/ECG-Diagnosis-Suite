# 🚀 部署助手 - 逐步操作指南

**你的情况**: 已有服务器
**目标**: 部署ECG智能诊断系统
**方式**: 逐步指导操作

---

## 📋 部署前确认

在开始之前，请确认以下信息：

### ❓ 请告诉我你的服务器信息

**需要的信息**:

1. **服务器操作系统**？
   - Ubuntu 20.04 / 22.04
   - CentOS 7 / 8
   - Debian 10 / 11
   - 其他

2. **服务器配置**？
   - CPU核心数
   - 内存大小
   - 硬盘空间

3. **你能SSH连接到服务器吗**？
   - 能连接
   - 不确定如何连接
   - 需要帮助

4. **你的服务器公网IP**？
   - 例如: 123.45.67.89

---

## 🚀 部署方式选择

### 方式A: 使用一键部署脚本 ⭐⭐⭐⭐⭐（推荐）

**最简单！我已经创建了自动化脚本**

**你只需要**:
1. SSH连接到服务器
2. 运行一个命令
3. 等待完成

**操作步骤**:
```bash
# 1. 连接到服务器
ssh root@你的服务器IP

# 2. 下载部署脚本
wget https://raw.githubusercontent.com/suizhaoqinggong/ECG-Diagnosis-Suite/main/deploy.sh

# 3. 运行脚本
bash deploy.sh

# 等待3-5分钟自动完成部署
```

---

### 方式B: 手动逐步操作 ⭐⭐⭐

**我来指导你每一步**

#### 第一步：连接服务器

```bash
# 在你的本地终端执行
ssh root@你的服务器IP

# 输入密码（不会显示，直接输入后按回车）
```

**遇到问题？**
- 提示"Connection refused": 检查安全组22端口是否开放
- 提示"Permission denied": 密码错误或使用密钥登录

---

#### 第二步：更新系统

连接成功后，执行：

```bash
# 更新软件包列表
apt update

# 升级已安装的包
apt upgrade -y

# 安装必要工具
apt install -y git curl wget vim
```

**如果系统不是Ubuntu**:
- CentOS: `yum update -y`
- Debian: 同Ubuntu

---

#### 第三步：安装Docker

```bash
# 安装Docker
curl -fsSL https://get.docker.com | bash

# 启动Docker
systemctl start docker
systemctl enable docker

# 验证安装
docker --version
```

**预期输出**: `Docker version 24.0.x, build xxxxxxx`

---

#### 第四步：安装Docker Compose

```bash
# 安装Docker Compose
apt install -y docker-compose

# 验证安装
docker-compose --version
```

---

#### 第五步：获取项目代码

```bash
# 创建目录
mkdir -p /opt/projects
cd /opt/projects

# 克隆项目
git clone https://github.com/suizhaoqinggong/ECG-Diagnosis-Suite.git

# 进入项目目录
cd ECG-Diagnosis-Suite
```

---

#### 第六步：配置环境变量

```bash
# 获取服务器IP（自动检测）
SERVER_IP=$(curl -s ifconfig.me)
echo "服务器IP: $SERVER_IP"

# 创建后端配置
cat > backend/.env << EOF
DEBUG=False
SECRET_KEY=$(openssl rand -hex 32)
DATABASE_URL=postgresql+asyncpg://ecg:ecg123456@db:5432/ecg_db
REDIS_URL=redis://redis:6379/0
DEVICE=cpu
MODEL_PATH=./models/weights/ecg_model.pth
CORS_ORIGINS=http://$SERVER_IP,http://localhost
EOF

# 创建前端配置
cat > frontend/.env << EOF
VITE_API_BASE_URL=http://$SERVER_IP:8000
EOF
```

---

#### 第七步：启动服务

```bash
# 确保在项目目录
cd /opt/projects/ECG-Diagnosis-Suite

# 启动所有服务
docker-compose up -d

# 查看启动状态
docker-compose ps

# 查看日志（如果有问题）
docker-compose logs -f
```

**预期看到**:
```
NAME          STATUS    PORTS
ecg-backend   running   0.0.0.0:8000->8000/tcp
ecg-frontend  running   0.0.0.0:80->80/tcp
ecg-db        running   5432/tcp
ecg-redis     running   6379/tcp
```

---

#### 第八步：验证部署

```bash
# 测试后端
curl http://localhost:8000/health

# 预期输出: {"status":"healthy"}
```

---

## 🌐 访问你的应用

部署成功后，在浏览器中访问：

```
前端界面: http://你的服务器IP
后端API: http://你的服务器IP:8000/docs
```

---

## 🔧 常用运维命令

### 查看服务状态
```bash
docker-compose ps
```

### 查看日志
```bash
# 查看所有服务日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f backend
docker-compose logs -f frontend
```

### 重启服务
```bash
docker-compose restart
```

### 停止服务
```bash
docker-compose down
```

### 更新代码
```bash
git pull
docker-compose up -d --build
```

---

## 🐛 常见问题排查

### 问题1: 端口无法访问

**检查**:
```bash
# 检查端口监听
netstat -tuln | grep -E '(80|8000)'

# 检查防火墙
ufw status

# 开放端口（如果需要）
ufw allow 80
ufw allow 8000
```

**云服务器检查**:
- 登录云服务商控制台
- 找到"安全组"配置
- 开放80和8000端口

---

### 问题2: Docker服务启动失败

```bash
# 查看Docker状态
systemctl status docker

# 重启Docker
systemctl restart docker

# 查看日志
journalctl -u docker
```

---

### 问题3: 容器启动失败

```bash
# 查看容器日志
docker-compose logs

# 检查配置
docker-compose config

# 重新构建
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

---

### 问题4: 内存不足

```bash
# 查看内存
free -h

# 如果内存不足，创建swap
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile
```

---

## 📞 接下来怎么做？

**现在告诉我**:

1. **你的服务器操作系统**（Ubuntu/CentOS/其他）
2. **你想使用哪种部署方式**:
   - A. 一键脚本（推荐，快速）
   - B. 手动操作（学习理解）

3. **或者直接告诉我**: "我已连接到服务器，开始部署"

**我会根据你的情况提供具体的命令指导！** 🚀

---

**创建时间**: 2026-03-12
**脚本位置**: `deploy.sh`
**状态**: ✅ 准备就绪
