# .dat文件上传使用指南

## ✅ 前端已更新

前端界面现在支持两种文件格式：
- 📸 **图片格式**: .png, .jpg, .jpeg
- 📁 **ECG数据**: .dat (PTB-XL格式)

## 📝 使用步骤

### 1. 准备文件

确保你有以下两个文件：
```
record.dat    # ECG信号数据
record.hea    # 头文件（包含元数据）
```

**重要**: 这两个文件必须在**同一目录**下，且文件名相同（只有扩展名不同）

### 2. 访问前端

打开浏览器访问：http://localhost:5173

### 3. 上传.dat文件

1. 点击上传区域
2. 在文件选择器中，选择 `.dat` 文件
3. 系统会自动检测这是.dat文件
4. 后端会自动查找同目录下的.hea文件

### 4. 查看结果

上传成功后，你会看到：
- 预测结果（如"心肌梗死"）
- 置信度百分比
- Top-3预测
- 完整的概率分布
- 医学建议

## ⚠️ 注意事项

### 文件要求
- ✅ 必须是PTB-XL格式（wfdb兼容）
- ✅ 必须有配套的.hea文件
- ✅ 两个文件必须在同一目录
- ✅ 文件名必须相同
- ✅ 文件大小不超过10MB

### 常见错误

#### 错误1: 缺少.hea文件
```
错误信息：缺少配套文件：Header file not found
解决方法：确保.dat和.hea文件在同一目录
```

#### 错误2: 文件格式不支持
```
错误信息：不支持的文件格式
解决方法：使用PTB-XL格式的.dat文件
```

#### 错误3: 信号格式无效
```
错误信息：信号数据格式无效
解决方法：检查.dat文件是否损坏，.hea文件格式是否正确
```

## 🧪 测试示例

### 使用curl测试

```bash
# 测试.dat文件上传
curl -X POST http://127.0.0.1:8000/api/diagnose \
  -F "file=@/path/to/record.dat"

# 返回示例
{
  "prediction": "心肌梗死",
  "confidence": 0.8289,
  "severity": "严重",
  "all_probabilities": {
    "正常": 0.0113,
    "心肌梗死": 0.8289,
    "ST-T改变": 0.0241,
    "传导障碍": 0.1030,
    "心室肥大": 0.0327
  }
}
```

### 使用Python测试

```python
import requests

# 上传.dat文件
files = {'file': open('record.dat', 'rb')}
response = requests.post('http://127.0.0.1:8000/api/diagnose', files=files)

# 打印结果
print(response.json())
```

## 💡 提示

1. **文件选择器**: 现在文件选择器会同时显示图片和.dat文件
2. **自动检测**: 系统会自动识别文件类型（图片或.dat）
3. **智能提示**: 上传.dat文件时会有提示信息
4. **错误处理**: 如果缺少.hea文件，会显示明确的错误信息

## 🎯 系统状态

- ✅ 后端API: http://127.0.0.1:8000
- ✅ 前端界面: http://localhost:5173
- ✅ API文档: http://127.0.0.1:8000/docs

---

**现在你可以在浏览器中打开 http://localhost:5173，上传.dat文件了！** 🎉
